# Dies ist eine Beispieldatei. Erstelle eine .env.production Datei (ohne .example)
# in deiner Build-Umgebung oder setze diese Variablen direkt in der Build-Umgebung.
# Nicht für sensible Daten verwenden, da sie in den Frontend-Code gelangen kann.

VITE_API_URL=https://your-production-api-domain.com/api/
